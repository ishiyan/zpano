{
  "name": "@zpano/entities",
  "version": "1.0.0",
  "description": "Financial trading data entities (Bar, Quote, Trade, Scalar) - TypeScript implementation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && jasmine --config=jasmine.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/jasmine": "^5.1.4",
    "@types/node": "^20.11.0",
    "jasmine": "^5.1.0",
    "typescript": "^5.3.3"
  }
}
