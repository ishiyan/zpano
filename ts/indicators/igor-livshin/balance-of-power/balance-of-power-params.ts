/** Describes parameters to create an instance of the Balance of Power indicator. */
export interface BalanceOfPowerParams {
  // Balance of Power requires OHLC bar data and has no configurable parameters.
}
