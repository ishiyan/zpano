/** Parameters of the Williams %R indicator. */
export interface WilliamsPercentRParams {
  /** The number of time periods. The typical values are 5, 9 or 14. The default is 14. Must be >= 2. */
  length: number;
}
