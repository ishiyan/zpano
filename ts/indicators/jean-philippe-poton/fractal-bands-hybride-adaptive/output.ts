/** Enumerates the outputs of the fractal bands hybride adaptive indicator. */
export enum FractalBandsHybrideAdaptiveOutput {
    /** FRASMA2 center line. */
    Frasma2Value = 0,
    /** Upper band. */
    Upper = 1,
    /** Lower band. */
    Lower = 2,
    /** Band (lower/upper pair). */
    Band = 3,
}
