/** Enumerates the outputs of the Fractal Bands indicator. */
export enum FractalBandsOutput {
    /** FRASMA2 center line. */
    Frasma2Value = 0,
    /** Upper band. */
    Upper = 1,
    /** Lower band. */
    Lower = 2,
    /** Band (lower/upper pair). */
    Band = 3,
}
