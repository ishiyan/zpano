/** Parameters of the Directional Indicator Minus indicator. */
export interface DirectionalIndicatorMinusParams {
  /** The smoothing length (the number of time periods). Must be >= 1. The default value is 14. */
  length: number;
}
