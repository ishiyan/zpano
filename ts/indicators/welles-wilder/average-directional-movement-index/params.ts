/** Parameters of the Average Directional Movement Index indicator. */
export interface AverageDirectionalMovementIndexParams {
  /** The smoothing length (the number of time periods). Must be >= 1. The default value is 14. */
  length: number;
}
