/** Enumerates outputs of the Sinc Wavelet Band-Pass indicator. */
export enum SincWaveletBandpassOutput {

  /** The band-passed price component (or its velocity). */
  SincWaveletBandpassValue = 0,
}
