/** Enumerates outputs of the Double Smoothed Stochastic indicator. */
export enum DoubleSmoothedStochasticOutput {

  /** The Double Smoothed Stochastic oscillator value (range [0, 100]). */
  DSSValue = 0,

  /** The signal-line value: the g-period SMA of the oscillator. */
  SignalValue = 1,
}
