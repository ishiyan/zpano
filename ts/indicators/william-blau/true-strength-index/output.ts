/** Enumerates outputs of the True Strength Index indicator. */
export enum TrueStrengthIndexOutput {

  /** The True Strength Index oscillator value (range [-100, +100]). */
  TSIValue = 0,

  /** The signal-line value: the ul-period EMA of the oscillator. */
  SignalValue = 1,
}
