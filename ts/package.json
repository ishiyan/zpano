{
  "name": "@zpano/ts",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Multi-module financial library — TypeScript implementation",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --loader ./loader.js node_modules/.bin/jasmine --config=jasmine.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/jasmine": "^5.1.4",
    "@types/node": "^20.11.0",
    "jasmine": "^5.1.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
