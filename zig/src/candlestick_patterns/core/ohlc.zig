// ---------------------------------------------------------------------------
// OHLC
// ---------------------------------------------------------------------------

/// A single bar's open, high, low, close values.
pub const OHLC = struct {
    o: f64,
    h: f64,
    l: f64,
    c: f64,
};
