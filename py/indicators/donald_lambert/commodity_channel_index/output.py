"""Commodity Channel Index output enum."""

from enum import IntEnum


class CommodityChannelIndexOutput(IntEnum):
    """Enumerates the outputs of the commodity channel index indicator."""

    VALUE = 0
